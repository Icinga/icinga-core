# SAMPLE ICINGA CONFIG SNIPPET FOR IDOMOD
#
# In order to have Icinga run the IDOMOD event broker module, you'll need
# to place a statement like the one found below in your main Icinga
# configuration file (icinga.cfg).  Adjust the paths, etc. to suit your needs.

broker_module=@bindir@/idomod.o config_file=@sysconfdir@/idomod.cfg

